apiVersion: s3.aws.m.upbound.io/v1beta1
kind: Bucket
metadata:
  name: {{ .Values.env.s3BucketName }}
  namespace: crossplane-system
spec:
  providerConfigRef:
    name: default
    kind: ProviderConfig
  forProvider:
    region: us-east-2
    tags:
      demo: crossplane-demo
      owner: devops
